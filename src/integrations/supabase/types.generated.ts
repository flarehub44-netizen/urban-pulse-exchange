/**
 * Regenerate after schema changes:
 *   npm run db:types
 *
 * Hand-maintained subset lives in types.ts; this file is the merge target for `supabase gen types`.
 */
export type { Database, MarketStatus, BetSide, Tables } from "./types";
